var nodeID3 = require('../index.js');
var fs = require('fs');

//tags.image is the path to the image (only png/jpeg files allowed)

//No image support atm
console.log({rofl: "'test"})
var files = fs.readdirSync('D:\\Google Drive\\Musik');
for(var i = 0; i < files.length; i++) {
if(files.indexOf('.mp3')) {
	var read = nodeID3.read('D:\\Google Drive\\Musik\\' + files[i]);
	if(read == false) {
		console.log(files[i]);
	}
	console.log(read);
}
}